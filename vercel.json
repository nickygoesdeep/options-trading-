{
  "$comment": "quant-engine cron deployment",
  "version": 2,
  "buildCommand": "cd packages/shared && npx tsc && cd ../../apps/engine && npx tsc",
  "crons": [
    {
      "path": "/api/cron/run-engine",
      "schedule": "*/5 * * * *"
    }
  ]
}
